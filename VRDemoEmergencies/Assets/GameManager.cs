using UnityEngine;

public class GameManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        AudioManager.instance.PlayBackgroundMusic("274545__raygunvirus__outdoor-industrial-fan-3");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
